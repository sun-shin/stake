{
  id: event.id,
  title: event.title,
  created_by: event.user_id,
  description: event.description,
  address: event.address,
  attendee_limit: event.attendee_limit,
  # event_start: event.event_start,
  # duration: 
  openings: event.openings,
  tags: event.tags.map do |tag|
    tag.name
  end
}